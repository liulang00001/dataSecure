/**
 register global derective here.
 */

import Vue from 'vue';
import btnPermissions from './btnPermission/index';
import throttle from './throttle/index';

Vue.use(btnPermissions);
Vue.use(throttle);
